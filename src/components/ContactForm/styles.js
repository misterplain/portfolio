const styles = {
    wrapper: (theme) => ({
        display: "flex",
        flexDirection: "column",
        padding: "15px 15px",
    }),
    formWrapper: (theme) => ({
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        padding: "20px"
    }),
    formGroup: (theme) => ({
        width: "80%",
        marginBottom: "30px",
    })
}

export default styles;