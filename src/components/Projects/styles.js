const styles = {
    wrapper: (theme) => ({
        display: "flex",
        flexDirection: "column",
        padding: "15px 15px",
    }),
    title: (theme) => ({
        fontWeight: 700,
    }),
    buttonWrapper: (theme) => ({
        display: "flex",
        flexDirection: "row",
        justifyContent: "center",
        marginTop: "20px",
    })
}

export default styles;